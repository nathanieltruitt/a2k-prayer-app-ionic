import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'A2K Prayer App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
